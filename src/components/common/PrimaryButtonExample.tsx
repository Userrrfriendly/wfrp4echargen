import PrimaryButton from './PrimaryButton';

/**
 * Example usage of PrimaryButton
 */
const PrimaryButtonExample = () => (
  <PrimaryButton onClick={() => alert('Clicked!')}>
    Example Primary Button
  </PrimaryButton>
);

export default PrimaryButtonExample;
